name: release

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build
        run: make
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          name: Latest Release
          tag_name: latest
          files: |
            helloworld
